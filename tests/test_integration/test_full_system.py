"""Full system integration tests for Business bel Arabi RAG."""

import asyncio
import pytest
import uuid
from datetime import datetime
from typing import Dict, Any
from unittest.mock import patch, AsyncMock, MagicMock

from app.main import app
from app.services.chat.ceo_expert import ceo_expert_service
from app.models.schemas import ChatRequest, ChatResponse, RecommendedContent
from app.utils.threading import run_in_executor, batch_process_async, active_requests, processed_requests


class TestFullSystemIntegration:
    """Full system integration tests."""
    
    @pytest.mark.asyncio
    async def test_complete_rag_workflow(self):
        """Test complete RAG workflow from query to response with recommendations."""
        
        # Mock all external services
        with patch('app.services.clients.gemini.google.generativeai') as mock_genai, \
             patch('app.services.memory.conversation.redis.Redis') as mock_redis, \
             patch('app.services.processing.vectorstore.QdrantClient') as mock_qdrant:
            
            # Setup mocks
            mock_genai.configure.return_value = None
            mock_model = MagicMock()
            mock_model.generate_content.return_value.text = "إليك نصائحي المتخصصة في ريادة الأعمال..."
            mock_genai.GenerativeModel.return_value = mock_model
            
            mock_redis_instance = MagicMock()
            mock_redis_instance.get.return_value = None
            mock_redis_instance.set.return_value = True
            mock_redis.return_value = mock_redis_instance
            
            mock_qdrant_instance = MagicMock()
            mock_qdrant_instance.search.return_value = [\n                {\n                    \"id\": \"chunk-1\",\n                    \"payload\": {\n                        \"content_id\": \"podcast-1\",\n                        \"content_type\": \"podcast\",\n                        \"text\": \"نصائح لبدء الأعمال التجارية...\",\n                        \"title\": \"بودكاست ريادة الأعمال\",\n                        \"excerpt\": \"نصائح عملية للرياديين...\"\n                    },\n                    \"score\": 0.89\n                }\n            ]\n            mock_qdrant.return_value = mock_qdrant_instance\n            \n            # Test the complete workflow\n            conversation_id = str(uuid.uuid4())\n            \n            response = await ceo_expert_service.chat_with_context(\n                message=\"كيف أبدأ شركتي الناشئة بنجاح؟\",\n                conversation_id=conversation_id,\n                use_context=True,\n                max_context_results=5,\n                include_podcast_recommendations=True\n            )\n            \n            # Verify complete response\n            assert isinstance(response, ChatResponse)\n            assert response.conversation_id == conversation_id\n            assert \"ريادة الأعمال\" in response.response or \"نصائحي\" in response.response\n            assert response.metadata[\"ceo_expert_version\"] == \"1.0.0\"\n    \n    @pytest.mark.asyncio\n    async def test_multi_user_concurrent_sessions(self):\n        \"\"\"Test multiple users with concurrent chat sessions.\"\"\"\n        \n        # Simulate multiple users\n        users = [\n            {\"id\": f\"user-{i}\", \"query\": f\"سؤال رقم {i} عن الأعمال\"}\n            for i in range(1, 16)  # 15 concurrent users\n        ]\n        \n        async def user_chat_session(user: Dict[str, str]) -> Dict[str, Any]:\n            \"\"\"Simulate a complete user chat session.\"\"\"\n            conversation_id = f\"conv-{user['id']}\"\n            \n            # Mock CEO Expert response\n            with patch.object(ceo_expert_service, 'chat_with_context') as mock_chat:\n                mock_chat.return_value = ChatResponse(\n                    response=f\"إجابة مخصصة للمستخدم {user['id']}\",\n                    conversation_id=conversation_id,\n                    sources=[],\n                    recommended_podcasts=[\n                        RecommendedContent(\n                            id=f\"rec-{user['id']}\",\n                            title=f\"بودكاست مقترح للمستخدم {user['id']}\",\n                            content_type=\"podcast\",\n                            similarity_score=0.8,\n                            excerpt=\"محتوى ذو صلة...\"\n                        )\n                    ],\n                    metadata={\n                        \"user_id\": user['id'],\n                        \"ceo_expert_version\": \"1.0.0\",\n                        \"recommendations_count\": 1\n                    }\n                )\n                \n                start_time = datetime.now()\n                response = await mock_chat(\n                    message=user['query'],\n                    conversation_id=conversation_id,\n                    use_context=True\n                )\n                processing_time = (datetime.now() - start_time).total_seconds()\n                \n                return {\n                    \"user\": user,\n                    \"response\": response,\n                    \"processing_time\": processing_time\n                }\n        \n        # Execute all user sessions concurrently\n        start_time = datetime.now()\n        results = await asyncio.gather(*[\n            user_chat_session(user) for user in users\n        ])\n        total_execution_time = (datetime.now() - start_time).total_seconds()\n        \n        # Verify all users got responses\n        assert len(results) == 15\n        for result in results:\n            assert \"response\" in result\n            assert isinstance(result[\"response\"], ChatResponse)\n            assert len(result[\"response\"].recommended_podcasts) == 1\n            assert result[\"response\"].metadata[\"ceo_expert_version\"] == \"1.0.0\"\n        \n        # Verify performance under load\n        assert total_execution_time < 10  # Should handle 15 users quickly\n        average_response_time = sum(r[\"processing_time\"] for r in results) / len(results)\n        assert average_response_time < 2  # Each response should be fast\n    \n    @pytest.mark.asyncio\n    async def test_thread_pool_stress_test(self):\n        \"\"\"Stress test thread pool with many concurrent operations.\"\"\"\n        \n        def cpu_intensive_task(task_id: int) -> Dict[str, Any]:\n            \"\"\"CPU-intensive task for stress testing.\"\"\"\n            import time\n            start = time.time()\n            \n            # Simulate heavy computation\n            result = 0\n            for i in range(task_id * 50000):\n                result += i ** 2\n            \n            return {\n                \"task_id\": task_id,\n                \"result\": result,\n                \"execution_time\": time.time() - start\n            }\n        \n        # Create many concurrent tasks\n        num_tasks = 50\n        tasks = [\n            run_in_executor(cpu_intensive_task, i)\n            for i in range(1, num_tasks + 1)\n        ]\n        \n        start_time = datetime.now()\n        results = await asyncio.gather(*tasks)\n        total_time = (datetime.now() - start_time).total_seconds()\n        \n        # Verify all tasks completed\n        assert len(results) == num_tasks\n        for i, result in enumerate(results, 1):\n            assert result[\"task_id\"] == i\n            assert isinstance(result[\"result\"], int)\n            assert result[\"execution_time\"] > 0\n        \n        # Thread pool should handle the load efficiently\n        assert total_time < 30  # Should complete in reasonable time\n        \n        # Verify no task took too long individually\n        max_task_time = max(r[\"execution_time\"] for r in results)\n        assert max_task_time < 5\n    \n    @pytest.mark.asyncio\n    async def test_memory_leak_prevention(self):\n        \"\"\"Test that the system doesn't have memory leaks under load.\"\"\"\n        \n        # Track initial state\n        initial_active = active_requests.value\n        initial_processed = processed_requests.value\n        \n        async def simulate_request() -> None:\n            \"\"\"Simulate a single request lifecycle.\"\"\"\n            active_requests.increment()\n            try:\n                # Simulate request processing\n                await asyncio.sleep(0.1)\n                processed_requests.increment()\n            finally:\n                active_requests.decrement()\n        \n        # Run many concurrent requests\n        tasks = [simulate_request() for _ in range(100)]\n        await asyncio.gather(*tasks)\n        \n        # Verify counters\n        assert active_requests.value == initial_active  # Should return to initial\n        assert processed_requests.value == initial_processed + 100  # Should have processed all\n    \n    @pytest.mark.asyncio\n    async def test_error_recovery_and_resilience(self):\n        \"\"\"Test system error recovery and resilience.\"\"\"\n        \n        # Test with intermittent failures\n        failure_count = 0\n        \n        async def flaky_operation(operation_id: int) -> str:\n            \"\"\"Operation that fails intermittently.\"\"\"\n            nonlocal failure_count\n            \n            if operation_id % 3 == 0:  # Fail every 3rd operation\n                failure_count += 1\n                raise Exception(f\"Simulated failure for operation {operation_id}\")\n            \n            await asyncio.sleep(0.05)  # Simulate processing\n            return f\"success-{operation_id}\"\n        \n        # Process many operations with some failures\n        operations = list(range(1, 31))  # 30 operations\n        \n        results = await batch_process_async(\n            items=operations,\n            process_func=flaky_operation,\n            batch_size=5,\n            max_concurrency=8\n        )\n        \n        # Verify resilience\n        successful_operations = len(results)\n        expected_failures = len([op for op in operations if op % 3 == 0])\n        expected_successes = len(operations) - expected_failures\n        \n        assert successful_operations == expected_successes\n        assert failure_count == expected_failures\n        \n        # Verify successful results\n        for result in results:\n            assert result.startswith(\"success-\")\n    \n    @pytest.mark.asyncio\n    async def test_api_endpoint_integration_with_threading(self):\n        \"\"\"Test API endpoints work correctly with multi-threading.\"\"\"\n        from fastapi.testclient import TestClient\n        \n        # Create test client\n        client = TestClient(app)\n        \n        # Mock authentication\n        with patch('app.core.security.verify_api_key', return_value=\"test-key\"), \\\n             patch.object(ceo_expert_service, 'chat_with_context') as mock_chat:\n            \n            # Setup mock response\n            mock_chat.return_value = ChatResponse(\n                response=\"إجابة من CEO Expert\",\n                conversation_id=\"api-test\",\n                sources=[{\"id\": \"source-1\", \"title\": \"مصدر تجريبي\"}],\n                recommended_podcasts=[\n                    RecommendedContent(\n                        id=\"podcast-test\",\n                        title=\"بودكاست تجريبي\",\n                        content_type=\"podcast\",\n                        similarity_score=0.85,\n                        excerpt=\"محتوى تجريبي للاختبار...\"\n                    )\n                ],\n                metadata={\n                    \"context_used\": True,\n                    \"recommendations_count\": 1,\n                    \"ceo_expert_version\": \"1.0.0\"\n                }\n            )\n            \n            # Test API endpoint\n            response = client.post(\n                \"/chat/\",\n                headers={\"Authorization\": \"Bearer test-key\"},\n                json={\n                    \"message\": \"كيف أطور مهاراتي في القيادة؟\",\n                    \"conversation_id\": \"api-test\",\n                    \"use_context\": True,\n                    \"max_context_results\": 5\n                }\n            )\n            \n            # Verify API response\n            assert response.status_code == 200\n            data = response.json()\n            \n            assert data[\"conversation_id\"] == \"api-test\"\n            assert data[\"response\"] == \"إجابة من CEO Expert\"\n            assert len(data[\"recommended_podcasts\"]) == 1\n            assert data[\"metadata\"][\"ceo_expert_version\"] == \"1.0.0\"\n            assert data[\"metadata\"][\"recommendations_count\"] == 1\n    \n    @pytest.mark.asyncio\n    async def test_health_monitoring_integration(self):\n        \"\"\"Test health monitoring with system statistics.\"\"\"\n        from app.routes.health import get_system_stats\n        \n        # Mock health check components\n        with patch('app.routes.health.memory_service') as mock_memory, \\\n             patch('app.routes.health.vector_store') as mock_vector:\n            \n            # Setup mocks\n            mock_memory.get_stats.return_value = {\n                \"conversations\": 25,\n                \"cache_hit_rate\": 0.85,\n                \"memory_usage\": \"45MB\"\n            }\n            \n            mock_vector.get_collection_info.return_value = {\n                \"points_count\": 1500,\n                \"status\": \"green\",\n                \"disk_usage\": \"120MB\"\n            }\n            \n            # Test health stats endpoint\n            stats = await get_system_stats()\n            \n            # Verify comprehensive stats\n            assert \"content_stats\" in stats\n            assert \"memory_stats\" in stats\n            assert \"vector_stats\" in stats\n            assert \"system_info\" in stats\n            \n            # Verify threading metrics\n            assert \"threading\" in stats[\"system_info\"]\n            assert \"max_workers\" in stats[\"system_info\"][\"threading\"]\n    \n    @pytest.mark.asyncio\n    async def test_concurrent_ingestion_and_chat(self):\n        \"\"\"Test concurrent ingestion and chat operations.\"\"\"\n        \n        # Mock ingestion process\n        async def mock_ingestion_task():\n            \"\"\"Simulate background ingestion.\"\"\"\n            await asyncio.sleep(0.5)  # Simulate ingestion time\n            return {\"status\": \"completed\", \"items_processed\": 50}\n        \n        # Mock chat requests during ingestion\n        async def mock_chat_during_ingestion(user_id: int):\n            \"\"\"Simulate chat request during ingestion.\"\"\"\n            with patch.object(ceo_expert_service, 'chat_with_context') as mock_chat:\n                mock_chat.return_value = ChatResponse(\n                    response=f\"Response during ingestion for user {user_id}\",\n                    conversation_id=f\"ingestion-chat-{user_id}\",\n                    sources=[],\n                    recommended_podcasts=[],\n                    metadata={\"during_ingestion\": True}\n                )\n                \n                return await mock_chat(\n                    message=f\"سؤال من المستخدم {user_id} أثناء الفهرسة\",\n                    conversation_id=f\"ingestion-chat-{user_id}\",\n                    use_context=True\n                )\n        \n        # Run ingestion and chat concurrently\n        ingestion_task = mock_ingestion_task()\n        chat_tasks = [mock_chat_during_ingestion(i) for i in range(5)]\n        \n        start_time = datetime.now()\n        ingestion_result, *chat_results = await asyncio.gather(\n            ingestion_task, *chat_tasks\n        )\n        execution_time = (datetime.now() - start_time).total_seconds()\n        \n        # Verify both ingestion and chat succeeded\n        assert ingestion_result[\"status\"] == \"completed\"\n        assert len(chat_results) == 5\n        \n        for result in chat_results:\n            assert isinstance(result, ChatResponse)\n            assert result.metadata[\"during_ingestion\"] is True\n        \n        # Should complete efficiently\n        assert execution_time < 2\n    \n    @pytest.mark.asyncio\n    async def test_recommendation_quality_and_relevance(self):\n        \"\"\"Test recommendation quality and contextual relevance.\"\"\"\n        \n        # Test queries with expected recommendation patterns\n        test_cases = [\n            {\n                \"query\": \"كيف أتعامل مع المستثمرين؟\",\n                \"expected_topics\": [\"استثمار\", \"تمويل\", \"مستثمرين\"]\n            },\n            {\n                \"query\": \"استراتيجيات التسويق الرقمي\",\n                \"expected_topics\": [\"تسويق\", \"رقمي\", \"إعلانات\"]\n            },\n            {\n                \"query\": \"كيف أدير فريق العمل؟\",\n                \"expected_topics\": [\"إدارة\", \"فريق\", \"قيادة\"]\n            }\n        ]\n        \n        for test_case in test_cases:\n            # Mock relevant podcast recommendations\n            mock_podcasts = [\n                RecommendedContent(\n                    id=f\"podcast-{i}\",\n                    title=f\"بودكاست عن {topic}\",\n                    content_type=\"podcast\",\n                    similarity_score=0.9 - (i * 0.1),\n                    excerpt=f\"محتوى ممتاز عن {topic}...\",\n                    tags=[topic]\n                )\n                for i, topic in enumerate(test_case[\"expected_topics\"])\n            ]\n            \n            with patch.object(ceo_expert_service, '_get_podcast_recommendations', return_value=mock_podcasts):\n                \n                recommendations = await ceo_expert_service._get_podcast_recommendations(\n                    query=test_case[\"query\"],\n                    limit=3\n                )\n                \n                # Verify recommendations are relevant\n                assert len(recommendations) == len(test_case[\"expected_topics\"])\n                \n                for rec in recommendations:\n                    assert rec.content_type == \"podcast\"\n                    assert rec.similarity_score > 0.7  # High relevance\n                    \n                    # Check if recommendation relates to query topics\n                    rec_text = rec.title.lower()\n                    has_relevant_topic = any(\n                        topic in rec_text for topic in test_case[\"expected_topics\"]\n                    )\n                    assert has_relevant_topic\n    \n    @pytest.mark.asyncio\n    async def test_system_scalability_metrics(self):\n        \"\"\"Test system scalability and performance metrics.\"\"\"\n        \n        # Test different load levels\n        load_levels = [10, 25, 50, 100]  # Number of concurrent operations\n        performance_results = {}\n        \n        for load_level in load_levels:\n            async def mock_operation(op_id: int) -> Dict[str, Any]:\n                \"\"\"Mock system operation.\"\"\"\n                start = datetime.now()\n                \n                # Simulate mixed I/O and CPU work\n                await asyncio.sleep(0.02)  # I/O simulation\n                await run_in_executor(lambda: sum(i for i in range(1000)), )  # CPU work\n                \n                return {\n                    \"operation_id\": op_id,\n                    \"processing_time\": (datetime.now() - start).total_seconds()\n                }\n            \n            # Execute load level\n            start_time = datetime.now()\n            tasks = [mock_operation(i) for i in range(load_level)]\n            results = await asyncio.gather(*tasks)\n            total_time = (datetime.now() - start_time).total_seconds()\n            \n            # Calculate metrics\n            avg_response_time = sum(r[\"processing_time\"] for r in results) / len(results)\n            throughput = len(results) / total_time\n            \n            performance_results[load_level] = {\n                \"total_time\": total_time,\n                \"avg_response_time\": avg_response_time,\n                \"throughput\": throughput,\n                \"success_rate\": 1.0  # All operations should succeed\n            }\n        \n        # Verify scalability characteristics\n        for load_level, metrics in performance_results.items():\n            # Response time should remain reasonable\n            assert metrics[\"avg_response_time\"] < 1\n            \n            # Throughput should scale with load (up to thread pool limit)\n            assert metrics[\"throughput\"] > 5  # Minimum throughput\n            \n            # All operations should succeed\n            assert metrics[\"success_rate\"] == 1.0\n        \n        # Higher loads should have better throughput (up to a point)\n        assert performance_results[50][\"throughput\"] > performance_results[10][\"throughput\"]\n    \n    @pytest.mark.asyncio\n    async def test_frontend_backend_data_flow(self):\n        \"\"\"Test complete data flow from frontend to backend.\"\"\"\n        \n        # Simulate frontend request data\n        frontend_request = {\n            \"message\": \"أريد نصائح للتوسع في السوق الخليجي\",\n            \"conversation_id\": \"frontend-flow-test\",\n            \"use_context\": True,\n            \"max_context_results\": 5\n        }\n        \n        # Mock complete backend response\n        expected_response = ChatResponse(\n            response=\"للتوسع في السوق الخليجي، عليك دراسة الثقافة المحلية...\",\n            conversation_id=\"frontend-flow-test\",\n            sources=[\n                {\"id\": \"source-1\", \"title\": \"دليل التوسع الإقليمي\"},\n                {\"id\": \"source-2\", \"title\": \"أسواق الخليج\"}\n            ],\n            recommended_podcasts=[\n                RecommendedContent(\n                    id=\"gulf-podcast\",\n                    title=\"التوسع في أسواق الخليج\",\n                    content_type=\"podcast\",\n                    similarity_score=0.92,\n                    excerpt=\"خبرات عملية في التوسع الإقليمي...\",\n                    tags=[\"توسع\", \"خليج\", \"أسواق\"]\n                )\n            ],\n            metadata={\n                \"context_used\": True,\n                \"sources_count\": 2,\n                \"recommendations_count\": 1,\n                \"processing_time_seconds\": 1.2,\n                \"ceo_expert_version\": \"1.0.0\"\n            }\n        )\n        \n        with patch.object(ceo_expert_service, 'chat_with_context', return_value=expected_response):\n            \n            # Simulate the complete request flow\n            response = await ceo_expert_service.chat_with_context(\n                message=frontend_request[\"message\"],\n                conversation_id=frontend_request[\"conversation_id\"],\n                use_context=frontend_request[\"use_context\"],\n                max_context_results=frontend_request[\"max_context_results\"]\n            )\n            \n            # Verify complete data flow\n            assert response.conversation_id == frontend_request[\"conversation_id\"]\n            assert \"التوسع في السوق الخليجي\" in response.response\n            assert len(response.sources) == 2\n            assert len(response.recommended_podcasts) == 1\n            assert \"خليج\" in response.recommended_podcasts[0].tags\n            \n            # Verify metadata richness for frontend display\n            metadata = response.metadata\n            assert \"context_used\" in metadata\n            assert \"sources_count\" in metadata\n            assert \"recommendations_count\" in metadata\n            assert \"processing_time_seconds\" in metadata\n            assert \"ceo_expert_version\" in metadata\n\n\n@pytest.mark.asyncio\nasync def test_complete_system_startup_and_shutdown():\n    \"\"\"Test complete system startup and shutdown sequence.\"\"\"\n    \n    # Mock system components\n    with patch('app.core.scheduler.scheduler_service') as mock_scheduler, \\\n         patch('app.main.executor') as mock_executor:\n        \n        # Mock scheduler\n        mock_scheduler.start.return_value = None\n        mock_scheduler.stop.return_value = None\n        \n        # Mock thread pool executor\n        mock_thread_pool = MagicMock()\n        mock_executor.shutdown.return_value = None\n        \n        # Test startup sequence\n        from app.main import lifespan\n        \n        async with lifespan(app):\n            # Verify startup completed\n            mock_scheduler.start.assert_called_once()\n            \n            # Test system is operational\n            assert app.title == \"Business bel Arabi RAG API\"\n            assert \"chat\" in [route.path for route in app.routes]\n            assert \"recommend\" in [route.path for route in app.routes]\n        \n        # Verify shutdown\n        mock_scheduler.stop.assert_called_once()\n\n\n@pytest.mark.asyncio\nasync def test_arabic_text_processing_integration():\n    \"\"\"Test Arabic text processing throughout the system.\"\"\"\n    \n    arabic_test_cases = [\n        \"كيف أبدأ مشروعي التجاري من الصفر؟\",\n        \"ما هي أفضل استراتيجيات التسويق للشركات الناشئة؟\",\n        \"كيف أجذب المواهب المتميزة لفريقي؟\",\n        \"نصائح لإدارة التدفق النقدي في الشركات الصغيرة\",\n        \"كيف أتعامل مع المنافسة الشديدة في السوق؟\"\n    ]\n    \n    # Test Arabic text processing in parallel\n    async def process_arabic_query(query: str) -> Dict[str, Any]:\n        \"\"\"Process Arabic query through the system.\"\"\"\n        \n        with patch.object(ceo_expert_service, 'chat_with_context') as mock_chat:\n            mock_chat.return_value = ChatResponse(\n                response=f\"إجابة مفصلة باللغة العربية للسؤال: {query[:30]}...\",\n                conversation_id=str(uuid.uuid4()),\n                sources=[],\n                recommended_podcasts=[],\n                metadata={\n                    \"language\": \"arabic\",\n                    \"query_length\": len(query),\n                    \"processing_successful\": True\n                }\n            )\n            \n            start_time = datetime.now()\n            response = await mock_chat(\n                message=query,\n                conversation_id=str(uuid.uuid4()),\n                use_context=True\n            )\n            processing_time = (datetime.now() - start_time).total_seconds()\n            \n            return {\n                \"query\": query,\n                \"response\": response,\n                \"processing_time\": processing_time\n            }\n    \n    # Process all Arabic queries concurrently\n    tasks = [process_arabic_query(query) for query in arabic_test_cases]\n    results = await asyncio.gather(*tasks)\n    \n    # Verify Arabic processing\n    assert len(results) == len(arabic_test_cases)\n    \n    for result in results:\n        response = result[\"response\"]\n        assert isinstance(response, ChatResponse)\n        assert \"إجابة مفصلة باللغة العربية\" in response.response\n        assert response.metadata[\"language\"] == \"arabic\"\n        assert response.metadata[\"processing_successful\"] is True\n        assert result[\"processing_time\"] < 2  # Should be fast\n\n\n@pytest.mark.asyncio\nasync def test_system_monitoring_and_diagnostics():\n    \"\"\"Test system monitoring and diagnostic capabilities.\"\"\"\n    \n    # Test system metrics collection\n    from app.utils.threading import active_requests, processed_requests, error_requests\n    \n    # Simulate system activity\n    async def simulate_system_activity():\n        \"\"\"Simulate various system activities.\"\"\"\n        activities = []\n        \n        # Simulate successful requests\n        for i in range(20):\n            active_requests.increment()\n            await asyncio.sleep(0.01)\n            processed_requests.increment()\n            active_requests.decrement()\n            activities.append(f\"success-{i}\")\n        \n        # Simulate some errors\n        for i in range(3):\n            active_requests.increment()\n            await asyncio.sleep(0.01)\n            error_requests.increment()\n            active_requests.decrement()\n            activities.append(f\"error-{i}\")\n        \n        return activities\n    \n    # Run activity simulation\n    initial_processed = processed_requests.value\n    initial_errors = error_requests.value\n    \n    activities = await simulate_system_activity()\n    \n    # Verify metrics tracking\n    assert processed_requests.value == initial_processed + 20\n    assert error_requests.value == initial_errors + 3\n    assert active_requests.value == 0  # Should return to zero\n    assert len(activities) == 23\n    \n    # Test metrics in health endpoint format\n    metrics = {\n        \"active_requests\": active_requests.value,\n        \"processed_requests\": processed_requests.value,\n        \"error_requests\": error_requests.value,\n        \"success_rate\": processed_requests.value / (processed_requests.value + error_requests.value)\n    }\n    \n    assert metrics[\"active_requests\"] == 0\n    assert metrics[\"processed_requests\"] > 0\n    assert metrics[\"success_rate\"] > 0.8  # High success rate
